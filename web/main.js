var blocks = document.getElementsByClassName('services-items');
var container = document.getElementsByClassName('services');
var hs = new HorizontalScroll.default({
	blocks : blocks,
	container: container,
	spring: 1,
});
